"use client"

import * as AspectRatioPrimitive from "@radix-ui/react-aspect-ratio"

/**
 * A component that constrains its content to a specific aspect ratio.
 * This component is a wrapper around the `AspectRatioPrimitive.Root` component.
 */
const AspectRatio = AspectRatioPrimitive.Root

export { AspectRatio }
